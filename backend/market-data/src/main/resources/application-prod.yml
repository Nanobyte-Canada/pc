server:
  forward-headers-strategy: framework

spring:
  jpa:
    show-sql: false

logging:
  level:
    com.portfolio: WARN
    com.portfolio.marketdata.questrade: INFO
    org.springframework.web: WARN
