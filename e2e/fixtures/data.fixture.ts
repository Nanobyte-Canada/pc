import { test as base } from '@playwright/test';

// Data fixture placeholder — will be expanded in Phase 2
// as test data needs are identified.

export const test = base;

export { expect } from '@playwright/test';
